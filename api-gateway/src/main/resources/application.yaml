server:
  port: 8080

spring:
  application:
    name: api-gateway
  threads:
    virtual:
      enabled: true

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: notes-service-route
              uri: http://localhost:8082
              predicates:
                - Path=/api/notes/**,/api/rooms/**